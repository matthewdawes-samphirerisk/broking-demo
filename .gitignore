# Dependencies
node_modules/

# Secrets — never commit
.env
.env.local
*.key
*.pem

# OS junk
.DS_Store
Thumbs.db
desktop.ini
~$*

# Editor / IDE
.vscode/
.idea/

# NOTE: the data files in data/ are deliberately committed.
# They are wholly synthetic and safe to publish.

# NOTE: public/data.js is generated by tools/build-data.py but IS committed.
# Railway's Node builder has no Python, so it cannot regenerate it at deploy time.
# Regenerate locally and commit the result whenever the spreadsheet changes.
